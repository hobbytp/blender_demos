# 灾难恢复与 RPO/RTO · 7 分钟完整课

2026-09-14：用户认可第七集样片并要求继续。沿用固定拓扑，增加目标比较、隔离核验、数据库边界、验收清单与失败对照。完整事实链和案例口径见 [第七集分镜](pve-ep07-disaster-recovery.md)。

## 入口

- 交互版：`http://127.0.0.1:5173/?sample=restore-lesson`。
- 完整成片：`out/disaster-recovery-7min-v1.mp4`，Composition `RestoreLesson`，420 秒 / 12600 帧 / 1920×1080 / 30 fps。
- 保留原样片：`out/disaster-recovery-30s-v1.mp4`，`?sample=restore`。

## 章节

| 时间 | 内容 | 可观察的变化 |
| --- | --- | --- |
| 0–35 | 恢复承诺与责任边界 | 磁盘、OS、数据库、业务四层证据依次揭示 |
| 35–85 | 恢复点与 RPO | 目标边界、失败候选、已选时间点和 30 分钟缺口 |
| 85–140 | 普通 VMA 恢复 | 归档解包、卷映射、数据写入、任务成功；Guest 仍未启动 |
| 140–185 | 隔离与启动 | 隔离与资源核验后，控制请求和读盘触发 OS 启动 |
| 185–240 | 数据库恢复 | 日志进入恢复过程，数据页重做，数据库就绪；再展开完整性条件 |
| 240–295 | 业务验收 | 查询经过应用和数据库往返；本次核对后其余检查仍待验收 |
| 295–355 | RTO 算例 | 六段用时逐步累积，45 分钟与 30 分钟目标比较 |
| 355–420 | 失败与回放 | 解包错误、PBS 独立对照；新恢复实例从头回放正常因果链 |

## 实现与小改入口

`src/RestorePilot.tsx` 提取 `RestoreScene`，由样片和完整课共享原有图形，配音分别由各自 wrapper 加载。`src/RestoreLesson.tsx` 负责原位展开与独立案例，`src/restore-lesson-timeline.ts` 负责真实词边界驱动的状态和事件。

`restore-lesson-narration.json`、`restore-lesson-audio.json` 和 `public/audio/quorum-restore-lesson/01–08.mp3` 是本课独立配音。八段时长为 28.632、46.008、50.736、42.840、51.120、51.168、53.664、63.096 秒，合计 387.264 秒，均在各自时间槽内。

命令：`npm.cmd run voice:restore-lesson`、`npm.cmd run build`、`npm.cmd test`、`npm.cmd run render:restore-lesson`。小改先定位章节、句子及 cue；保留其他课程和旧成片，修改配音时重新测量相关时间依赖。当前配音生成器仍按整个 edition 工作，不假定存在单段参数。

## 当前验收

- 构建通过；21 项 Node 测试通过。新增检查覆盖正常/回放因果顺序、查询逐跳时序、12600 帧前后拖动确定性、失败候选不入选、RPO/RTO 算术以及不提前宣称全部业务恢复。
- 八章及关键状态的浏览器跳转、倒退、旁白、静音、暂停、末帧 12599 停止和重播通过；页面错误 0。
- 共用播放器的 inputProps 稳定修复保留；本课连续播放采样无音频倒退或重叠。回归脚本 `tests/player-audio.browser.js`，它独立于 Node 测试。
- 已渲染 15、76、120、160、230、282、334、364、377、405 秒代表帧，检查主体布局、说明文字与独立案例。半透明文字表示尚未讲到的内容，不是缺失字形。
- 完整 MP4 已完成：H.264，1920×1080，30 fps，12600 帧；视频、AAC 48 kHz 音轨与容器均为 420.000000 秒。文件 20,915,144 字节，SHA256 `BCEBD21FF5C59E1F8E4570CD5694DD4CCC8AD6D2B1BDFCC21FD9F07AB2AAAE0A`。
- 全片音视频解码通过，成功提取 23 个关键时刻的实际成片帧；人工查看其中 76、334、364、405 秒的 RPO、RTO、失败与回放画面，正文和字幕无裁切，独立案例没有继承成功颜色。
- 将实际 MP4 解码为 PCM，与八段原始旁白逐段全长比对（排除每段两端各 0.1 秒）；相关系数最低 0.999955，检测到的偏移全部为 0 秒。导出旁白与原文段顺序匹配；网页连续播放采样也未发现音频倒退或重叠。这是自动比对与采样证据，不代表人工完整听审。

### 本次导出恢复记录

初次 Remotion 导出在第 10653 帧附近读取第八段音频失败：`localhost:3000` 返回了另一个 WSL 项目的资源目录和 404。没有修改或终止其他项目。使用独立端口 3317，并复用已缓存的前 9750 帧，从 9750 至 12599 补渲染；全部 JPEG 完整性检查通过后，用已安装的 FFmpeg 将 12600 帧与按原时间轴放置的八段旁白合成为最终成片。补渲染沿用相同源代码、分辨率与 JPEG 质量 80，没有以静帧替代动画。

常规 `render:restore-lesson` 命令已指定 `--port=3317`。本机恢复过程与验收证据保留在忽略目录 `out/`：`resume-restore-lesson.mjs`、`encode-restore-lesson.py`、`check-restore-lesson-media.ps1`、`check-restore-audio.py`、`restore-lesson-final-qa.log` 和 `restore-lesson-metadata.json`。常规重新导出仍使用上面的 npm 命令；MP4 和帧缓存不提交 Git。

没有实际执行 VM 恢复、失败注入、数据库恢复或业务生产切换。RPO/RTO 是明确口径的教学算例；画面的验证通过是示意，真实部署仍需独立证据。
